---
- hosts: webservers 
  become: True
  tasks:
    - name: Deploy PetClinic app to Web server
      copy:
        src: /var/jenkins_home/workspace/jenkins_job/target/spring-petclinic-3.0.0-SNAPSHOT.jar
        dest: /usr/local/petclinic.jar
    - name: Run PetClinic JAR file
      command: java -Dserver.port=50001 -jar /usr/local/petclinic.jar
